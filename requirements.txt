# Local (CPU) stack — pinned floors; exact pins exported via `pip freeze` after
# the environment is validated (see methodology/DECISOES_METODOLOGICAS.md).
# Install with:  py -3.12 -m venv .venv ; .venv\Scripts\pip install -r requirements.txt
pandas>=2.1
numpy>=1.26
pyarrow>=15.0
pyyaml>=6.0
pydantic>=2.5
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
emoji>=2.10
datasketch>=1.6
lingua-language-detector>=2.0
fasttext-wheel>=0.9.2
optuna>=3.5
mlflow>=2.11
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66

# Optional feature/serving extras (install as needed):
#   sentence-transformers>=2.6      # multilingual embeddings baseline (pulls torch CPU)
#   fastapi>=0.110 uvicorn[standard]>=0.29 slowapi>=0.1.9   # product API
#   gradio>=4.20                    # web demo

# Dev:
#   pytest ruff black isort pre-commit nbstripout
