# GPU stack for Google Colab (Fase 8 neural fine-tuning).
# Colab already ships torch + CUDA; we pin the HF stack on top.
# In a Colab cell:  !pip install -q -r requirements-colab.txt
transformers>=4.40
datasets>=2.18
accelerate>=0.29
evaluate>=0.4
sentencepiece>=0.2        # required by XLM-R / BERTimbau tokenizers
scikit-learn>=1.4
pandas>=2.1
pyarrow>=15.0
pyyaml>=6.0
mlflow>=2.11              # log runs back to Drive-mounted store
seqeval>=1.2
